<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    cacheDirectory="./storage/psalm"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="https://getpsalm.org/schema/config"
>
    <projectFiles>
        <directory name="app"/>
        <ignoreFiles>
            <directory name="vendor"/>
            <directory name="storage"/>
            <directory name="bootstrap/cache"/>
        </ignoreFiles>
    </projectFiles>

    <!-- Plugins can be added later if needed -->
</psalm>
